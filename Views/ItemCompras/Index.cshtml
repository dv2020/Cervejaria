@model IEnumerable<Cervejaria.Models.ItemCompra>

@{
    ViewBag.Title = "Index";
}

<h2 style="margin-left:10px">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fornecedor.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produto.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fornecedor.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produto.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemCompraId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemCompraId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemCompraId })
        </td>
    </tr>
}

</table>
